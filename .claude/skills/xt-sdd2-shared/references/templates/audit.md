# 审计报告：<卷宗 slug>

> - 卷宗：`<YYYY-MM-DD-<缩写>-<slug>>`
> - 审计对象 HEAD：`<git rev-parse --short HEAD>`
> - 审计时间：<YYYY-MM-DD HH:mm>

## 判定

<!-- 只允许两个值：PASS 或 FAIL。禁止"整改后通过""基本通过""有条件通过"等协商态措辞。 -->
<!-- 缺口整改后 MUST 重跑完整 audit 生成新报告替换本文件，禁止在旧报告上追加局部更新。 -->

**FAIL**

## 三方闭环校验

机器可核对，每一格给出证据指针（文件:行 / commit hash），缺证据即该行 FAIL：

| R-ID | tasks 覆盖位置 | commit（含卷宗前缀） | 结果 |
|------|---------------|---------------------|------|
| R-login-api-001 | tasks/01:第3步 | `a1b2c3d` | ✅ |
| R-login-api-00X | （无对应） | — | ❌ 缺失 |

反向核对：tasks/commits 中是否存在没有任何 R-ID 支撑的多余工作项：<无 / 列出>

## 测试证据

- 命令：`npm test`（来自 project.md frontmatter）
- 结果摘要：<通过/失败计数>，关键失败输出摘录于下（如有）
- 编译/lint：`<build_command>` <通过/失败>

## 代码审查结论

<requesting-code-review 流程产出的问题列表及处置：已修 / 登记为风险>

## 已知风险登记

<!-- 整改项之外仍然存在的接受风险；空则写"无"。 -->

| 风险描述 | 接受理由 | 后续动作 |
|----------|---------|---------|
| | | |

## 缺口清单（FAIL 时必填）

- [ ] <缺口 1：什么缺失、修复动作是什么>
